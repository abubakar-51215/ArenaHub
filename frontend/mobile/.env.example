# FastAPI backend base URL (no trailing slash, no /api/v1 suffix).
# Simulator / web: http://localhost:8000
# Physical device: use your machine's LAN IP, e.g. http://192.168.1.5:8000
EXPO_PUBLIC_API_URL=http://localhost:8000
