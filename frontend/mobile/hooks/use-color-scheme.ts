/**
 * The app ships a single light theme to match the product wireframes exactly,
 * so the color scheme is pinned to `light` regardless of the OS setting.
 */
export function useColorScheme(): 'light' {
  return 'light';
}
