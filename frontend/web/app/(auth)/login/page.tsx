/** Placeholder — the real login form lands in Sprint 2 (web auth UI). */
export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-6">
      <p className="text-muted-foreground">Login — coming in Sprint 2.</p>
    </main>
  );
}
